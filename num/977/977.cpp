#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;
//那么数组平方的最大值就在数组的两端，不是最左边就是最右边，不可能是中间
class Solution {
public:
    vector<int> sortedSquares(vector<int>& nums) {
       int i=0;
       int j=nums.size()-1;
        
    }
};
