//层序遍历二叉树，用队列
#include <iostream>
#include <queue>
#include <vector>
using namespace std;
class Solution {
public:
    vector<vector<int>> levelOrder(TreeNode* root) {
        queue<TreeNode*> que;
        if(root!=NULL) que.push(root);//先把根节点压入队列
        vector<int> result;


        return result;
    }
};
